name: upeu_flutter_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^1.7.0
  firebase_auth: ^3.1.3
  equatable: ^2.0.3
  flutter_bloc: ^7.3.0
  google_sign_in: ^5.0.2
  flutter_hooks: ^0.18.0
  cupertino_icons: ^1.0.2
  flutter_facebook_auth: ^3.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
